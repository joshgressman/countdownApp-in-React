import React from 'react'; // allows you to use react and all of the components
import ReactDOM from 'react-dom'; // controls react code on the browser

ReactDOM.render(
<div><h1>countdownApp</h1></div>, document.getElementById('root') //targets id in html displays to DOM
);
